package vs.course.content.environment
{
	import starling.display.Image;
	import starling.textures.Texture;

	public class Environment extends Image
	{
		public function Environment( texture:Texture)
		{
			super( texture ); 
		}
	}
}